(import (scheme base) (scheme write))

(define foo 1)
(define bar 1)
(let ((baz 1))
  (define-syntax m1
    (syntax-rules ()
      ((_) (list foo bar baz))))

  (set! foo 2)
  (let ((bar 2) (baz 2))
    (write (m1))))
