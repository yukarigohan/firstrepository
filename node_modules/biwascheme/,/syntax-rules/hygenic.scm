(define-syntax swap!
  (syntax-rules ()
    ((_ a b)
     (let ((temp a))
       (set! a b)
       (set! b temp)))))

(define temp 1)
(define b 2)
(swap! temp b)
(write (list temp b))

(print (%macroexpand (swap! temp b)))
