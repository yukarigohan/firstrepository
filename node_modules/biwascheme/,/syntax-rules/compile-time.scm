; 1. コンパイルタイムに副作用が起こるマクロを書いたらどうなるのか
; 2. 

(define (bar)
  (display "hello"))

(define-syntax foo
  (er-macro-transformer
    (lambda (expr rename id=?)
      ;(display "hello")
      (bar)
      (cadr expr))))

(display (foo 1234))

; % csc compile-time.scm
; % ./compile-time
; 1234
; % csc compile-time.scm
; hello
