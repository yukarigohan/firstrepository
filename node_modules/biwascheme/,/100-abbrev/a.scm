(define-macro (some-macro-with-long-name x)
  "ok")

(define-macro (abbrev short long)
  `(define-macro (,short . args)
     `(,',long ,@args)))

;(abbrev smwln some-macro-with-long-name)

(abbrev mylet let)

;(display (smwln 'foo))

(mylet ((a 1)) (display a))

;(define short "short")
;(define long "long")
;(write
;  `(define-macro (,short . args)
;     `(,',long ,@args)))
