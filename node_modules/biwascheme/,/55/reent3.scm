(import (scheme base) (scheme lazy) (scheme write))
(define flag #f)
(define p (delay (if flag
                   1
                   (begin
                     (set! flag #t)
                     (force p)
                     2))))
(display (force p))  ;=> should be 1
