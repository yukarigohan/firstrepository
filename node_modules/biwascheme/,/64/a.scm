;(define (make-list n val)
;   (if (zero? n)
;       '()
;       (cons val (make-list (- n 1)))))
;(print (make-list 2 #t))


(define (make-list n val)
   (if (zero? n)
       '()
       (cons val (make-list (- n 1) val))))

(print (make-list 2 #t))
