# Syntax

<a name="syntax"/>

* Comment
  * Single line comment (`;`)
  * Multi line comment (`#| ... |#`) (srfi-30)
  * One sexp comment (`#;`) (srfi-62)
* Quote
  * quote (`'`)
  * quasiquote (\`) unquote (`,`) unquote-splicing (`,@`)
